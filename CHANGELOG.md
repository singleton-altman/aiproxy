# Changelog

本文件记录 AI Proxy Mobile 的重要版本变更。项目采用语义化版本规范。

## [Unreleased]

## [0.0.9] - 2026-07-30

### 修复

- 修复 iOS 原生标签栏与页面安全区重复处理造成的底部越界滚动和整屏空白。
- 首页“今日 / 本周 / 本月”改为本地自然周期统计，请求趋势固定展示含今天在内的近 7 个自然日。
- 聊天消息和模型、协议、系统提示词等会话设置按服务与账号隔离保存，重新进入后自动恢复。
- 时间范围测试改为兼容 GitHub Actions 使用的 Node.js 20，避免构建阶段因直接导入 TypeScript 文件失败。

### 工程

- 从正式包依赖中移除 Expo Dev Client、开发菜单和 Expo OTA Updates，并删除未使用的 EAS 在线构建配置；GitHub APK 与无证书 IPA 原生构建保持不变。

## [0.0.8] - 2026-07-29

### 工程

- 加固 GitHub Android APK 构建：限制 Gradle 单 worker、原生构建失败后自动重试一次，并在失败时上传完整诊断日志及输出日志摘要。

## [0.0.7] - 2026-07-29

### 修复

- 修复 Expo 57 原生端账号导入与预检的文件上传兼容问题，并改用原始字节保存账号导出文件，避免 Blob API 缺失导致导入、检查和导出失败。
- 首页与统计页的 API Key 用量关联密钥目录，优先显示真实 Key 名称或脱敏前缀，不再使用数字序号代替名称。

### 优化

- 全局辅助文字统一为 11px，原有 12px 及更小字号同步调整，并修正图表刻度、徽章与紧凑列表的可读性。
- 优化共享图标块、刷新按钮和侧栏选中图标的边框、阴影与按压反馈；账号导入入口改用独立上传图标。

## [0.0.6] - 2026-07-29

### 修复

- 管理首页“今日全站”改为按本地自然日统计，并在重新进入页面及停留期间自动刷新，避免近 24 小时数据与旧缓存造成偏差。
- 修正全局页面和独立列表的滚动边界，避免导航栏 inset 重复计算、底部继续空滑及 Android 滑动后内容被错误裁剪；页面底部间距统一收紧至 12px。
- 修复请求热力图错误复用旧分析分桶、缺失星期字段时数据全部落入周日以及页面停留期间不更新的问题；热力图改用最新事件计算并每 15 秒自动刷新。

### 优化

- 重设计全站开关控件，统一紧凑尺寸、滑块动画、点击热区、禁用状态和无障碍语义，避免 iOS 原生开关在列表中比例过大。
- 额度卡增加账号启用开关，支持单次点击直接启用或禁用账号，并同步更新额度与账号缓存。
- 放大管理入口卡片的副标题文字并增强字重与行高，提高小屏设备上的可读性。
- 供应商配额分组与账号卡改为显示对应品牌图标，支持深浅主题，并为自定义供应商保留彩色字标回退。
- 供应商配额的账号脱敏改为单一眼睛图标按钮，并将额度、状态、时间、套餐及操作区小字统一提升至 12px；通用页面副标题同步提升至 12px。
- Providers 列表项点击后直接进入编辑表单，移除详情中转层及其易误触的删除入口。
- 上游账号卡片增加实体启用/禁用按钮，单击即可切换账号状态，并在请求期间锁定重复操作。
- 首页与完整统计页增加“按 API Key”用量维度，仅显示 Key 名称或脱敏前缀；重做用户、供应商、账号及 Key 统计的视觉层级，标签和明细字号统一提升至至少 12px。
- 收紧首页 9 张用量指标卡的高度、内边距及纵向间距，实时流量 3 项在常见手机宽度下改为同排展示，移除末项独占整行造成的额外空白。

## [0.0.5] - 2026-07-29

### 修复

- 修复供应商配额未解析 `quota.windows`，导致已获取的账号额度、余额与重置时间无法显示的问题；刷新后直接采用服务端返回的最新分组数据。

### 新增

- 供应商配额页增加可持久化的账号脱敏开关，支持邮箱、普通账号和手机号显示脱敏。

## [0.0.4] - 2026-07-29

### 修复

- 上游账号“测试全部模型”改为可取消的串行测试，并补充执行互斥、进度反馈和异常兜底，避免批量测试时应用退出。
- 修复上游账号、模型管理与用户管理页面的移动端搜索框被压缩后与筛选控件重叠的问题。
- 未连接服务时不再展示反向代理返回的 HTML 源码，并将首页重复错误合并为简洁的连接提示。

### 调整

- 首页恢复 `0.0.3` 改版前的圆角统计卡、独立实时流量、排行与分项明细布局，同时保留前台刷新、自动轮询和禁用统计缓存。
- 系统管理信息改为紧凑双列布局，版本更新区移除重复的缓存与自更新技术状态，仅保留版本对比和必要操作。

## [0.0.3] - 2026-07-28

### 修复

- 首页概览改为前台进入即刷新并按数据类型自动轮询，同时禁用统计请求缓存，避免显示旧数据。
- 重构上游账号列表，按 Web 规则显示账号名称、提供商、套餐、出口、状态和最后使用时间，修复禁用账号仍显示正常及账号前缀错误。
- 对齐账号状态判定、需处理统计、恢复与重新登录操作，并阻止专属出口代理被重复分配。
- 修复可用模型弹层错误提供模型编辑入口的问题，补充不支持目录时的手动模型测试。

### 新增

- 首页按紧凑双列指标卡、日期范围、分组使用分布和请求趋势重新排版，并支持 Token 与实际费用切换。
- 增加账号状态与供应商筛选、30 秒自动刷新、文件导入、原生文件导出和单账号操作菜单。
- 增加定时预热配置，支持执行时间、时区、模型建议和最近执行结果。
- 编辑账号时可选择已有出口代理，并保留高级出口选择器与系统维护状态。

### 工程

- 移除当前 Web 与服务端不再使用的账号额度重置路径，并增加回归契约测试。
- 增加 `expo-sharing`，用于在 iOS 与 Android 分享账号导出文件。

## [0.0.2] - 2026-07-28

### 修复

- 将上游账号的可用模型响应改为可搜索、复制和测试的模型列表，并支持进入对应模型编辑表单。
- 解除 API Key 列表对请求日志的阻塞依赖，修复密钥与模型页首次进入持续加载的问题。
- 移除用户编辑弹层的重复键盘避让，修复输入框聚焦后页面过度位移的问题。

### 工程

- 补充账号模型测试接口契约检查。

## [0.0.1] - 2026-07-28

### 新增

- 基于 Expo 和 React Native 的 iOS、Android 移动客户端。
- Session、API Key 和管理令牌认证。
- API Key、模型、套餐、额度、用量和请求记录等用户端流程。
- 用户、账号、供应商、模型、套餐、统计、配置和系统操作等管理端流程。
- OpenAI、Anthropic 聊天、流式响应和图片生成。
- Android APK、iOS 无证书 IPA 和 EAS Build 工作流。

### 优化

- 统一移动端界面、响应式列表、键盘处理、安全区和弹层交互。
- 对齐当前服务端与 Web 管理端的接口契约。
